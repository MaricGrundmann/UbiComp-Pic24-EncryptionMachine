#include "cipher.h"

void Cipher_Init(const char *password, uint8_t len) {
    (void)password;
    (void)len;
}

void Cipher_Process(uint8_t *buffer, uint16_t len) {
    (void)buffer;
    (void)len;
}
